/*
 * This is a React component that renders the html of the options of a question.
 * Each option is a button that when clicked, sends the option to the parent component.
 * It receives the option by context provider
 */

function getOption